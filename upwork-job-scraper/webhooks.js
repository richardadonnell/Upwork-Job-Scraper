var addToActivityLog = function(message) {
  // ... (inline the function from activity_log.js)
};

var sendToWebhook = function(newJobs) {
  // ... (no changes needed)
};

var isValidUrl = function(string) {
  // ... (no changes needed)
};