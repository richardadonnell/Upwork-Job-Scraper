var addToActivityLog = function(message) {
  // ... (inline the function from activity_log.js)
};

var processJobs = async function(newJobs) {
  // ... (inline the function from job_processing.js)
};

var loadFeedSourceSettings = function() {
  // ... (inline the function from feed_sources.js)
};

var selectedFeedSource = "most-recent"; // ... (inline the variable from extension_state.js)
var customSearchUrl = ""; // ... (inline the variable from extension_state.js)
var jobScrapingEnabled = true; // ... (inline the variable from extension_state.js)

var logAndReportError = function(message, error) {
  // ... (inline the function from error_handling.js)
};

var checkForNewJobs = async function() {
  // ... (no changes needed)
};

var scrapeJobs = function() {
  // ... (no changes needed)
};